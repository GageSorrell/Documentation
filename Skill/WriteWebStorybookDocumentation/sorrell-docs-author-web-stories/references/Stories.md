# Web stories

Use typed CSF metadata with a `component` field whenever the story documents a
React component. Prefer named stories that describe reader-visible states.
Provide stable args so controls and autodocs can infer useful examples.

Keep story assets imported or relative to the story. Do not depend on a
filesystem path or a development-only URL.
