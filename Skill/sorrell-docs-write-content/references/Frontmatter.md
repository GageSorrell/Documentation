# Frontmatter

Every article should include:

```yaml
title: A reader-facing title
description: A one-sentence search description
group: Guides
order: 10
draft: false
```

Keep ids and filenames stable after publication. Moving an article requires a
redirect in the site's normalized configuration.
