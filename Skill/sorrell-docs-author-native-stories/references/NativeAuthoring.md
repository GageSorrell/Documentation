# Native authoring

Stories belong under `Stories`, examples under `Examples`, and articles under
`Articles`. Prefer public package imports and keep story names stable so deep
links and review notes remain useful.

Use the shared provider and decorators generated by `sorrell-docs story init`.
Do not add a second navigation, persistence, or theme implementation inside a
single story.
